name: Deploy to Nethely (production)

on:
    push:
        branches: ['production']

jobs:
    deploy:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Setup SSH key with passphrase
              run: |
                  mkdir -p ~/.ssh
                  echo "${{ secrets.NETHELY_SSH_KEY }}" > ~/.ssh/id_rsa
                  chmod 600 ~/.ssh/id_rsa

                  # Start ssh-agent
                  eval "$(ssh-agent -s)"

                  # Add the private key with passphrase from secrets
                  ssh-add <(echo "${{ secrets.NETHELY_SSH_KEY }}") <<< "${{ secrets.NETHELY_SSH_PASSPHRASE }}"

            - name: Trust Nethely host key
              run: |
                  ssh-keyscan -p "${{ secrets.NETHELY_SSH_PORT }}" "${{ secrets.NETHELY_SSH_HOST }}" >> ~/.ssh/known_hosts
                  chmod 600 ~/.ssh/known_hosts

            - name: Deploy via rsync
              run: |
                  rsync -az --delete \
                    --exclude='.git/' \
                    --exclude='.github/' \
                    --exclude='.env' \
                    --exclude='.env.*' \
                    -e "ssh -p ${{ secrets.NETHELY_SSH_PORT }}" \
                    ./ "${{ secrets.NETHELY_SSH_USER }}@${{ secrets.NETHELY_SSH_HOST }}:${{ secrets.NETHELY_DEPLOY_PATH }}"
