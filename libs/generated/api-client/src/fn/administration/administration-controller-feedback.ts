/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AdministrationResponseDto } from '../../models/administration-response-dto';
import { Object } from '../../models/object';

export interface AdministrationControllerFeedback$Params {
  limit?: Object;
  cursor?: string;
  q?: string;
  kind?: 'bug' | 'idea';
  severity?: 'low' | 'medium' | 'high';
  status?: 'open' | 'in_progress' | 'resolved' | 'closed';
}

export function administrationControllerFeedback(http: HttpClient, rootUrl: string, params?: AdministrationControllerFeedback$Params, context?: HttpContext): Observable<StrictHttpResponse<AdministrationResponseDto>> {
  const rb = new RequestBuilder(rootUrl, administrationControllerFeedback.PATH, 'get');
  if (params) {
    rb.query('limit', params.limit, {});
    rb.query('cursor', params.cursor, {});
    rb.query('q', params.q, {});
    rb.query('kind', params.kind, {});
    rb.query('severity', params.severity, {});
    rb.query('status', params.status, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AdministrationResponseDto>;
    })
  );
}

administrationControllerFeedback.PATH = '/api/v1/admin/feedback';
