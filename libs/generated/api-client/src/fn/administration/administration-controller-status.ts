/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AdministrationResponseDto } from '../../models/administration-response-dto';
import { UpdateUserStatusDto } from '../../models/update-user-status-dto';

export interface AdministrationControllerStatus$Params {
  id: string;
      body: UpdateUserStatusDto
}

export function administrationControllerStatus(http: HttpClient, rootUrl: string, params: AdministrationControllerStatus$Params, context?: HttpContext): Observable<StrictHttpResponse<AdministrationResponseDto>> {
  const rb = new RequestBuilder(rootUrl, administrationControllerStatus.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AdministrationResponseDto>;
    })
  );
}

administrationControllerStatus.PATH = '/api/v1/admin/users/{id}/status';
