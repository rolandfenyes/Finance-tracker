/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AdministrationResponseDto } from '../../models/administration-response-dto';
import { Object } from '../../models/object';

export interface AdministrationControllerUsers$Params {
  limit?: Object;
  cursor?: string;
  q?: string;
  role?: 'free' | 'premium' | 'admin';
  status?: 'active' | 'inactive';
  verified?: 'true' | 'false';
}

export function administrationControllerUsers(http: HttpClient, rootUrl: string, params?: AdministrationControllerUsers$Params, context?: HttpContext): Observable<StrictHttpResponse<AdministrationResponseDto>> {
  const rb = new RequestBuilder(rootUrl, administrationControllerUsers.PATH, 'get');
  if (params) {
    rb.query('limit', params.limit, {});
    rb.query('cursor', params.cursor, {});
    rb.query('q', params.q, {});
    rb.query('role', params.role, {});
    rb.query('status', params.status, {});
    rb.query('verified', params.verified, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AdministrationResponseDto>;
    })
  );
}

administrationControllerUsers.PATH = '/api/v1/admin/users';
