/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PreviewEmailTemplateDto } from '../../models/preview-email-template-dto';

export interface NotificationsAdminControllerPreview$Params {
  code: string;
      body: PreviewEmailTemplateDto
}

export function notificationsAdminControllerPreview(http: HttpClient, rootUrl: string, params: NotificationsAdminControllerPreview$Params, context?: HttpContext): Observable<StrictHttpResponse<{
}>> {
  const rb = new RequestBuilder(rootUrl, notificationsAdminControllerPreview.PATH, 'post');
  if (params) {
    rb.path('code', params.code, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<{
      }>;
    })
  );
}

notificationsAdminControllerPreview.PATH = '/api/v1/admin/email-templates/{code}/preview';
