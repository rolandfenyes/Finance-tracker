/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BillingResponseDto } from '../../models/billing-response-dto';

export interface BillingControllerPromotion$Params {
  id: string;
}

export function billingControllerPromotion(http: HttpClient, rootUrl: string, params: BillingControllerPromotion$Params, context?: HttpContext): Observable<StrictHttpResponse<BillingResponseDto>> {
  const rb = new RequestBuilder(rootUrl, billingControllerPromotion.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BillingResponseDto>;
    })
  );
}

billingControllerPromotion.PATH = '/api/v1/admin/billing/promotions/{id}';
