/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BillingResponseDto } from '../../models/billing-response-dto';
import { TrialPromotionDto } from '../../models/trial-promotion-dto';

export interface BillingControllerTrial$Params {
      body: TrialPromotionDto
}

export function billingControllerTrial(http: HttpClient, rootUrl: string, params: BillingControllerTrial$Params, context?: HttpContext): Observable<StrictHttpResponse<BillingResponseDto>> {
  const rb = new RequestBuilder(rootUrl, billingControllerTrial.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BillingResponseDto>;
    })
  );
}

billingControllerTrial.PATH = '/api/v1/admin/billing/promotions/trial';
