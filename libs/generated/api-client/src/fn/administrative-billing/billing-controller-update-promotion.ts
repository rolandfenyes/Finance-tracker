/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BillingResponseDto } from '../../models/billing-response-dto';
import { PromotionDto } from '../../models/promotion-dto';

export interface BillingControllerUpdatePromotion$Params {
  id: string;
      body: PromotionDto
}

export function billingControllerUpdatePromotion(http: HttpClient, rootUrl: string, params: BillingControllerUpdatePromotion$Params, context?: HttpContext): Observable<StrictHttpResponse<BillingResponseDto>> {
  const rb = new RequestBuilder(rootUrl, billingControllerUpdatePromotion.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BillingResponseDto>;
    })
  );
}

billingControllerUpdatePromotion.PATH = '/api/v1/admin/billing/promotions/{id}';
