/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BasicIncomesResponseDto } from '../../models/basic-incomes-response-dto';

export interface BudgetingControllerBasicIncomes$Params {
}

export function budgetingControllerBasicIncomes(http: HttpClient, rootUrl: string, params?: BudgetingControllerBasicIncomes$Params, context?: HttpContext): Observable<StrictHttpResponse<BasicIncomesResponseDto>> {
  const rb = new RequestBuilder(rootUrl, budgetingControllerBasicIncomes.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BasicIncomesResponseDto>;
    })
  );
}

budgetingControllerBasicIncomes.PATH = '/api/v1/basic-incomes';
