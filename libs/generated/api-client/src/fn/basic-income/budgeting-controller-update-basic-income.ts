/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BasicIncomesResponseDto } from '../../models/basic-incomes-response-dto';
import { UpdateBasicIncomeDto } from '../../models/update-basic-income-dto';

export interface BudgetingControllerUpdateBasicIncome$Params {
  id: string;
      body: UpdateBasicIncomeDto
}

export function budgetingControllerUpdateBasicIncome(http: HttpClient, rootUrl: string, params: BudgetingControllerUpdateBasicIncome$Params, context?: HttpContext): Observable<StrictHttpResponse<BasicIncomesResponseDto>> {
  const rb = new RequestBuilder(rootUrl, budgetingControllerUpdateBasicIncome.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BasicIncomesResponseDto>;
    })
  );
}

budgetingControllerUpdateBasicIncome.PATH = '/api/v1/basic-incomes/{id}';
