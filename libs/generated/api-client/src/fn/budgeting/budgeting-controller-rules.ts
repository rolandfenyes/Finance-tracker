/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BudgetRulesResponseDto } from '../../models/budget-rules-response-dto';

export interface BudgetingControllerRules$Params {

/**
 * When supplied, includes forecast-income planning and signed rule variance for this month.
 */
  month?: string;
}

export function budgetingControllerRules(http: HttpClient, rootUrl: string, params?: BudgetingControllerRules$Params, context?: HttpContext): Observable<StrictHttpResponse<BudgetRulesResponseDto>> {
  const rb = new RequestBuilder(rootUrl, budgetingControllerRules.PATH, 'get');
  if (params) {
    rb.query('month', params.month, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BudgetRulesResponseDto>;
    })
  );
}

budgetingControllerRules.PATH = '/api/v1/budget-rules';
