/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BudgetRulesResponseDto } from '../../models/budget-rules-response-dto';
import { UpdateBudgetRuleDto } from '../../models/update-budget-rule-dto';

export interface BudgetingControllerUpdateRule$Params {
  id: string;
      body: UpdateBudgetRuleDto
}

export function budgetingControllerUpdateRule(http: HttpClient, rootUrl: string, params: BudgetingControllerUpdateRule$Params, context?: HttpContext): Observable<StrictHttpResponse<BudgetRulesResponseDto>> {
  const rb = new RequestBuilder(rootUrl, budgetingControllerUpdateRule.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BudgetRulesResponseDto>;
    })
  );
}

budgetingControllerUpdateRule.PATH = '/api/v1/budget-rules/{id}';
