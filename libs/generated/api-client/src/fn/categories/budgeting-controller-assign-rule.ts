/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AssignBudgetRuleDto } from '../../models/assign-budget-rule-dto';
import { CategoriesResponseDto } from '../../models/categories-response-dto';

export interface BudgetingControllerAssignRule$Params {
  id: string;
      body: AssignBudgetRuleDto
}

export function budgetingControllerAssignRule(http: HttpClient, rootUrl: string, params: BudgetingControllerAssignRule$Params, context?: HttpContext): Observable<StrictHttpResponse<CategoriesResponseDto>> {
  const rb = new RequestBuilder(rootUrl, budgetingControllerAssignRule.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CategoriesResponseDto>;
    })
  );
}

budgetingControllerAssignRule.PATH = '/api/v1/categories/{id}/budget-rule';
