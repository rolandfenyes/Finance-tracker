/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CategoriesResponseDto } from '../../models/categories-response-dto';

export interface BudgetingControllerCategories$Params {
}

export function budgetingControllerCategories(http: HttpClient, rootUrl: string, params?: BudgetingControllerCategories$Params, context?: HttpContext): Observable<StrictHttpResponse<CategoriesResponseDto>> {
  const rb = new RequestBuilder(rootUrl, budgetingControllerCategories.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CategoriesResponseDto>;
    })
  );
}

budgetingControllerCategories.PATH = '/api/v1/categories';
