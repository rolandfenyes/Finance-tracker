/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { EmergencyReserveResponseDto } from '../../models/emergency-reserve-response-dto';
import { ReverseEmergencyReserveMovementDto } from '../../models/reverse-emergency-reserve-movement-dto';

export interface EmergencyReserveControllerReverse$Params {
  id: string;
  'Idempotency-Key': string;
      body: ReverseEmergencyReserveMovementDto
}

export function emergencyReserveControllerReverse(http: HttpClient, rootUrl: string, params: EmergencyReserveControllerReverse$Params, context?: HttpContext): Observable<StrictHttpResponse<EmergencyReserveResponseDto>> {
  const rb = new RequestBuilder(rootUrl, emergencyReserveControllerReverse.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.header('Idempotency-Key', params['Idempotency-Key'], {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<EmergencyReserveResponseDto>;
    })
  );
}

emergencyReserveControllerReverse.PATH = '/api/v1/emergency-reserve/movements/{id}/reversals';
