/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { EmergencyReserveResponseDto } from '../../models/emergency-reserve-response-dto';
import { UpdateEmergencyReserveTargetDto } from '../../models/update-emergency-reserve-target-dto';

export interface EmergencyReserveControllerUpdateTarget$Params {
      body: UpdateEmergencyReserveTargetDto
}

export function emergencyReserveControllerUpdateTarget(http: HttpClient, rootUrl: string, params: EmergencyReserveControllerUpdateTarget$Params, context?: HttpContext): Observable<StrictHttpResponse<EmergencyReserveResponseDto>> {
  const rb = new RequestBuilder(rootUrl, emergencyReserveControllerUpdateTarget.PATH, 'put');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<EmergencyReserveResponseDto>;
    })
  );
}

emergencyReserveControllerUpdateTarget.PATH = '/api/v1/emergency-reserve/target';
