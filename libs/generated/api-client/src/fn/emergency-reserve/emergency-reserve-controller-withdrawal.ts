/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateEmergencyReserveMovementDto } from '../../models/create-emergency-reserve-movement-dto';
import { EmergencyReserveResponseDto } from '../../models/emergency-reserve-response-dto';

export interface EmergencyReserveControllerWithdrawal$Params {
  'Idempotency-Key': string;
      body: CreateEmergencyReserveMovementDto
}

export function emergencyReserveControllerWithdrawal(http: HttpClient, rootUrl: string, params: EmergencyReserveControllerWithdrawal$Params, context?: HttpContext): Observable<StrictHttpResponse<EmergencyReserveResponseDto>> {
  const rb = new RequestBuilder(rootUrl, emergencyReserveControllerWithdrawal.PATH, 'post');
  if (params) {
    rb.header('Idempotency-Key', params['Idempotency-Key'], {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<EmergencyReserveResponseDto>;
    })
  );
}

emergencyReserveControllerWithdrawal.PATH = '/api/v1/emergency-reserve/withdrawals';
