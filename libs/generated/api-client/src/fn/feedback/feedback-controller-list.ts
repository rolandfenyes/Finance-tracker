/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { FeedbackResponseDto } from '../../models/feedback-response-dto';
import { Object } from '../../models/object';

export interface FeedbackControllerList$Params {
  limit?: Object;
  cursor?: string;
  status?: 'open' | 'in_progress' | 'resolved' | 'closed';
  kind?: 'bug' | 'idea';
}

export function feedbackControllerList(http: HttpClient, rootUrl: string, params?: FeedbackControllerList$Params, context?: HttpContext): Observable<StrictHttpResponse<FeedbackResponseDto>> {
  const rb = new RequestBuilder(rootUrl, feedbackControllerList.PATH, 'get');
  if (params) {
    rb.query('limit', params.limit, {});
    rb.query('cursor', params.cursor, {});
    rb.query('status', params.status, {});
    rb.query('kind', params.kind, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<FeedbackResponseDto>;
    })
  );
}

feedbackControllerList.PATH = '/api/v1/feedback';
