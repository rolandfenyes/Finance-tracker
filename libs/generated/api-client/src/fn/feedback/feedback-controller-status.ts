/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { FeedbackResponseDto } from '../../models/feedback-response-dto';
import { UpdateOwnedFeedbackStatusDto } from '../../models/update-owned-feedback-status-dto';

export interface FeedbackControllerStatus$Params {
  id: string;
      body: UpdateOwnedFeedbackStatusDto
}

export function feedbackControllerStatus(http: HttpClient, rootUrl: string, params: FeedbackControllerStatus$Params, context?: HttpContext): Observable<StrictHttpResponse<FeedbackResponseDto>> {
  const rb = new RequestBuilder(rootUrl, feedbackControllerStatus.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<FeedbackResponseDto>;
    })
  );
}

feedbackControllerStatus.PATH = '/api/v1/feedback/{id}/status';
