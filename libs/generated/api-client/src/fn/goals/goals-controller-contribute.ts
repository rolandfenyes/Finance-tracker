/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateGoalContributionDto } from '../../models/create-goal-contribution-dto';
import { GoalResponseDto } from '../../models/goal-response-dto';

export interface GoalsControllerContribute$Params {
  id: string;
  'Idempotency-Key': string;
      body: CreateGoalContributionDto
}

export function goalsControllerContribute(http: HttpClient, rootUrl: string, params: GoalsControllerContribute$Params, context?: HttpContext): Observable<StrictHttpResponse<GoalResponseDto>> {
  const rb = new RequestBuilder(rootUrl, goalsControllerContribute.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.header('Idempotency-Key', params['Idempotency-Key'], {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<GoalResponseDto>;
    })
  );
}

goalsControllerContribute.PATH = '/api/v1/goals/{id}/contributions';
