/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { GoalResponseDto } from '../../models/goal-response-dto';
import { ReverseGoalContributionDto } from '../../models/reverse-goal-contribution-dto';

export interface GoalsControllerReverse$Params {
  goalId: string;
  id: string;
  'Idempotency-Key': string;
      body: ReverseGoalContributionDto
}

export function goalsControllerReverse(http: HttpClient, rootUrl: string, params: GoalsControllerReverse$Params, context?: HttpContext): Observable<StrictHttpResponse<GoalResponseDto>> {
  const rb = new RequestBuilder(rootUrl, goalsControllerReverse.PATH, 'post');
  if (params) {
    rb.path('goalId', params.goalId, {});
    rb.path('id', params.id, {});
    rb.header('Idempotency-Key', params['Idempotency-Key'], {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<GoalResponseDto>;
    })
  );
}

goalsControllerReverse.PATH = '/api/v1/goals/{goalId}/contributions/{id}/reversals';
