/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { GoalsResponseDto } from '../../models/goals-response-dto';
import { UpdateGoalDto } from '../../models/update-goal-dto';

export interface GoalsControllerUpdate$Params {
  id: string;
      body: UpdateGoalDto
}

export function goalsControllerUpdate(http: HttpClient, rootUrl: string, params: GoalsControllerUpdate$Params, context?: HttpContext): Observable<StrictHttpResponse<GoalsResponseDto>> {
  const rb = new RequestBuilder(rootUrl, goalsControllerUpdate.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<GoalsResponseDto>;
    })
  );
}

goalsControllerUpdate.PATH = '/api/v1/goals/{id}';
