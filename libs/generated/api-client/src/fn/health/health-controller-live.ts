/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { LivenessResponse } from '../../models/liveness-response';

export interface HealthControllerLive$Params {
}

export function healthControllerLive(http: HttpClient, rootUrl: string, params?: HealthControllerLive$Params, context?: HttpContext): Observable<StrictHttpResponse<LivenessResponse>> {
  const rb = new RequestBuilder(rootUrl, healthControllerLive.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<LivenessResponse>;
    })
  );
}

healthControllerLive.PATH = '/api/v1/health/live';
