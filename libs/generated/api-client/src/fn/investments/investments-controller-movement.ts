/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateInvestmentMovementDto } from '../../models/create-investment-movement-dto';
import { InvestmentResponseDto } from '../../models/investment-response-dto';

export interface InvestmentsControllerMovement$Params {
  id: string;
  'Idempotency-Key': string;
      body: CreateInvestmentMovementDto
}

export function investmentsControllerMovement(http: HttpClient, rootUrl: string, params: InvestmentsControllerMovement$Params, context?: HttpContext): Observable<StrictHttpResponse<InvestmentResponseDto>> {
  const rb = new RequestBuilder(rootUrl, investmentsControllerMovement.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.header('Idempotency-Key', params['Idempotency-Key'], {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<InvestmentResponseDto>;
    })
  );
}

investmentsControllerMovement.PATH = '/api/v1/investments/{id}/movements';
