/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { InvestmentResponseDto } from '../../models/investment-response-dto';
import { ReverseInvestmentMovementDto } from '../../models/reverse-investment-movement-dto';

export interface InvestmentsControllerReverseMovement$Params {
  investmentId: string;
  id: string;
  'Idempotency-Key': string;
      body: ReverseInvestmentMovementDto
}

export function investmentsControllerReverseMovement(http: HttpClient, rootUrl: string, params: InvestmentsControllerReverseMovement$Params, context?: HttpContext): Observable<StrictHttpResponse<InvestmentResponseDto>> {
  const rb = new RequestBuilder(rootUrl, investmentsControllerReverseMovement.PATH, 'post');
  if (params) {
    rb.path('investmentId', params.investmentId, {});
    rb.path('id', params.id, {});
    rb.header('Idempotency-Key', params['Idempotency-Key'], {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<InvestmentResponseDto>;
    })
  );
}

investmentsControllerReverseMovement.PATH = '/api/v1/investments/{investmentId}/movements/{id}/reversals';
