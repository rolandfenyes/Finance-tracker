/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CorrectJournalEntryDto } from '../../models/correct-journal-entry-dto';
import { JournalCorrectionResponseDto } from '../../models/journal-correction-response-dto';

export interface LedgerControllerCorrect$Params {
  id: string;
  'Idempotency-Key': string;
      body: CorrectJournalEntryDto
}

export function ledgerControllerCorrect(http: HttpClient, rootUrl: string, params: LedgerControllerCorrect$Params, context?: HttpContext): Observable<StrictHttpResponse<JournalCorrectionResponseDto>> {
  const rb = new RequestBuilder(rootUrl, ledgerControllerCorrect.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.header('Idempotency-Key', params['Idempotency-Key'], {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<JournalCorrectionResponseDto>;
    })
  );
}

ledgerControllerCorrect.PATH = '/api/v1/journal/entries/{id}/corrections';
