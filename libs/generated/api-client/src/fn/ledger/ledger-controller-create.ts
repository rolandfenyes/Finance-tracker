/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateJournalEntryDto } from '../../models/create-journal-entry-dto';
import { JournalEntryResponseDto } from '../../models/journal-entry-response-dto';

export interface LedgerControllerCreate$Params {
  'Idempotency-Key': string;
      body: CreateJournalEntryDto
}

export function ledgerControllerCreate(http: HttpClient, rootUrl: string, params: LedgerControllerCreate$Params, context?: HttpContext): Observable<StrictHttpResponse<JournalEntryResponseDto>> {
  const rb = new RequestBuilder(rootUrl, ledgerControllerCreate.PATH, 'post');
  if (params) {
    rb.header('Idempotency-Key', params['Idempotency-Key'], {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<JournalEntryResponseDto>;
    })
  );
}

ledgerControllerCreate.PATH = '/api/v1/journal/entries';
