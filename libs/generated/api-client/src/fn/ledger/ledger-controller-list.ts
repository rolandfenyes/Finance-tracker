/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { JournalListResponseDto } from '../../models/journal-list-response-dto';

export interface LedgerControllerList$Params {
  dateFrom?: string;
  dateTo?: string;
  limit?: number;
  cursor?: string;
}

export function ledgerControllerList(http: HttpClient, rootUrl: string, params?: LedgerControllerList$Params, context?: HttpContext): Observable<StrictHttpResponse<JournalListResponseDto>> {
  const rb = new RequestBuilder(rootUrl, ledgerControllerList.PATH, 'get');
  if (params) {
    rb.query('dateFrom', params.dateFrom, {});
    rb.query('dateTo', params.dateTo, {});
    rb.query('limit', params.limit, {});
    rb.query('cursor', params.cursor, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<JournalListResponseDto>;
    })
  );
}

ledgerControllerList.PATH = '/api/v1/journal/entries';
