/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { JournalEntryResponseDto } from '../../models/journal-entry-response-dto';
import { ReverseJournalEntryDto } from '../../models/reverse-journal-entry-dto';

export interface LedgerControllerReverse$Params {
  id: string;
  'Idempotency-Key': string;
      body: ReverseJournalEntryDto
}

export function ledgerControllerReverse(http: HttpClient, rootUrl: string, params: LedgerControllerReverse$Params, context?: HttpContext): Observable<StrictHttpResponse<JournalEntryResponseDto>> {
  const rb = new RequestBuilder(rootUrl, ledgerControllerReverse.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.header('Idempotency-Key', params['Idempotency-Key'], {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<JournalEntryResponseDto>;
    })
  );
}

ledgerControllerReverse.PATH = '/api/v1/journal/entries/{id}/reversals';
