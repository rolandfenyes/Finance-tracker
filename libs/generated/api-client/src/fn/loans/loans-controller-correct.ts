/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateLoanPaymentDto } from '../../models/create-loan-payment-dto';
import { LoanResponseDto } from '../../models/loan-response-dto';

export interface LoansControllerCorrect$Params {
  loanId: string;
  id: string;
  'Idempotency-Key': string;
      body: CreateLoanPaymentDto
}

export function loansControllerCorrect(http: HttpClient, rootUrl: string, params: LoansControllerCorrect$Params, context?: HttpContext): Observable<StrictHttpResponse<LoanResponseDto>> {
  const rb = new RequestBuilder(rootUrl, loansControllerCorrect.PATH, 'post');
  if (params) {
    rb.path('loanId', params.loanId, {});
    rb.path('id', params.id, {});
    rb.header('Idempotency-Key', params['Idempotency-Key'], {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<LoanResponseDto>;
    })
  );
}

loansControllerCorrect.PATH = '/api/v1/loans/{loanId}/payments/{id}/corrections';
