/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateLoanDto } from '../../models/create-loan-dto';
import { LoansResponseDto } from '../../models/loans-response-dto';

export interface LoansControllerCreate$Params {
      body: CreateLoanDto
}

export function loansControllerCreate(http: HttpClient, rootUrl: string, params: LoansControllerCreate$Params, context?: HttpContext): Observable<StrictHttpResponse<LoansResponseDto>> {
  const rb = new RequestBuilder(rootUrl, loansControllerCreate.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<LoansResponseDto>;
    })
  );
}

loansControllerCreate.PATH = '/api/v1/loans';
