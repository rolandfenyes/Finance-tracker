/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { LoansResponseDto } from '../../models/loans-response-dto';

export interface LoansControllerList$Params {
}

export function loansControllerList(http: HttpClient, rootUrl: string, params?: LoansControllerList$Params, context?: HttpContext): Observable<StrictHttpResponse<LoansResponseDto>> {
  const rb = new RequestBuilder(rootUrl, loansControllerList.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<LoansResponseDto>;
    })
  );
}

loansControllerList.PATH = '/api/v1/loans';
