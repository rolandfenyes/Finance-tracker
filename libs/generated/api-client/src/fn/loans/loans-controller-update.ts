/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { LoansResponseDto } from '../../models/loans-response-dto';
import { UpdateLoanDto } from '../../models/update-loan-dto';

export interface LoansControllerUpdate$Params {
  id: string;
      body: UpdateLoanDto
}

export function loansControllerUpdate(http: HttpClient, rootUrl: string, params: LoansControllerUpdate$Params, context?: HttpContext): Observable<StrictHttpResponse<LoansResponseDto>> {
  const rb = new RequestBuilder(rootUrl, loansControllerUpdate.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<LoansResponseDto>;
    })
  );
}

loansControllerUpdate.PATH = '/api/v1/loans/{id}';
