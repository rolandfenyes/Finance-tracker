/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateDeletionRequestDto } from '../../models/create-deletion-request-dto';
import { PrivacyRequestResponseDto } from '../../models/privacy-request-response-dto';

export interface PrivacyControllerCreateDeletion$Params {

/**
 * Stable key for this destructive account-deletion request
 */
  'Idempotency-Key': string;
      body: CreateDeletionRequestDto
}

export function privacyControllerCreateDeletion(http: HttpClient, rootUrl: string, params: PrivacyControllerCreateDeletion$Params, context?: HttpContext): Observable<StrictHttpResponse<PrivacyRequestResponseDto>> {
  const rb = new RequestBuilder(rootUrl, privacyControllerCreateDeletion.PATH, 'post');
  if (params) {
    rb.header('Idempotency-Key', params['Idempotency-Key'], {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PrivacyRequestResponseDto>;
    })
  );
}

privacyControllerCreateDeletion.PATH = '/api/v1/privacy/deletion-requests';
