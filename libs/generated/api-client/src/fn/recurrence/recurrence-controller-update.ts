/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RecurringRulesResponseDto } from '../../models/recurring-rules-response-dto';
import { UpdateRecurringRuleDto } from '../../models/update-recurring-rule-dto';

export interface RecurrenceControllerUpdate$Params {
  id: string;
      body: UpdateRecurringRuleDto
}

export function recurrenceControllerUpdate(http: HttpClient, rootUrl: string, params: RecurrenceControllerUpdate$Params, context?: HttpContext): Observable<StrictHttpResponse<RecurringRulesResponseDto>> {
  const rb = new RequestBuilder(rootUrl, recurrenceControllerUpdate.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RecurringRulesResponseDto>;
    })
  );
}

recurrenceControllerUpdate.PATH = '/api/v1/recurring-rules/{id}';
