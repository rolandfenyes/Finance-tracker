/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { MonthReportResponseDto } from '../../models/month-report-response-dto';

export interface ReportingControllerCurrent$Params {
  kind?: 'income' | 'expense' | 'transfer' | 'adjustment' | 'trade_cash';
  categoryId?: string;
  currency?: string;
  query?: string;
  minAmount?: string;
  maxAmount?: string;
  limit?: number;
  cursor?: string;
}

export function reportingControllerCurrent(http: HttpClient, rootUrl: string, params?: ReportingControllerCurrent$Params, context?: HttpContext): Observable<StrictHttpResponse<MonthReportResponseDto>> {
  const rb = new RequestBuilder(rootUrl, reportingControllerCurrent.PATH, 'get');
  if (params) {
    rb.query('kind', params.kind, {});
    rb.query('categoryId', params.categoryId, {});
    rb.query('currency', params.currency, {});
    rb.query('query', params.query, {});
    rb.query('minAmount', params.minAmount, {});
    rb.query('maxAmount', params.maxAmount, {});
    rb.query('limit', params.limit, {});
    rb.query('cursor', params.cursor, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<MonthReportResponseDto>;
    })
  );
}

reportingControllerCurrent.PATH = '/api/v1/reports/months/current';
