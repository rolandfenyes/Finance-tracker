/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { YearReportResponseDto } from '../../models/year-report-response-dto';

export interface ReportingControllerYear$Params {
  year: number;
}

export function reportingControllerYear(http: HttpClient, rootUrl: string, params: ReportingControllerYear$Params, context?: HttpContext): Observable<StrictHttpResponse<YearReportResponseDto>> {
  const rb = new RequestBuilder(rootUrl, reportingControllerYear.PATH, 'get');
  if (params) {
    rb.path('year', params.year, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<YearReportResponseDto>;
    })
  );
}

reportingControllerYear.PATH = '/api/v1/reports/years/{year}';
