/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SecuritiesResponseDto } from '../../models/securities-response-dto';

export interface SecuritiesControllerPrices$Params {
  id: string;
  from: string;
  to: string;
}

export function securitiesControllerPrices(http: HttpClient, rootUrl: string, params: SecuritiesControllerPrices$Params, context?: HttpContext): Observable<StrictHttpResponse<SecuritiesResponseDto>> {
  const rb = new RequestBuilder(rootUrl, securitiesControllerPrices.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
    rb.query('from', params.from, {});
    rb.query('to', params.to, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SecuritiesResponseDto>;
    })
  );
}

securitiesControllerPrices.PATH = '/api/v1/securities/instruments/{id}/prices';
