/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CompleteTutorialDto } from '../../models/complete-tutorial-dto';
import { OnboardingResponseDto } from '../../models/onboarding-response-dto';

export interface UsersControllerCompleteTutorial$Params {
      body: CompleteTutorialDto
}

export function usersControllerCompleteTutorial(http: HttpClient, rootUrl: string, params: UsersControllerCompleteTutorial$Params, context?: HttpContext): Observable<StrictHttpResponse<OnboardingResponseDto>> {
  const rb = new RequestBuilder(rootUrl, usersControllerCompleteTutorial.PATH, 'patch');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<OnboardingResponseDto>;
    })
  );
}

usersControllerCompleteTutorial.PATH = '/api/v1/users/me/onboarding';
