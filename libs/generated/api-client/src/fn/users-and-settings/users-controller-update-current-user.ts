/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CurrentUserResponseDto } from '../../models/current-user-response-dto';
import { UpdateCurrentUserDto } from '../../models/update-current-user-dto';

export interface UsersControllerUpdateCurrentUser$Params {
      body: UpdateCurrentUserDto
}

export function usersControllerUpdateCurrentUser(http: HttpClient, rootUrl: string, params: UsersControllerUpdateCurrentUser$Params, context?: HttpContext): Observable<StrictHttpResponse<CurrentUserResponseDto>> {
  const rb = new RequestBuilder(rootUrl, usersControllerUpdateCurrentUser.PATH, 'patch');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CurrentUserResponseDto>;
    })
  );
}

usersControllerUpdateCurrentUser.PATH = '/api/v1/users/me';
