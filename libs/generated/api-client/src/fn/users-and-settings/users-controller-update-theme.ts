/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ThemePreferencesResponseDto } from '../../models/theme-preferences-response-dto';
import { UpdateThemeDto } from '../../models/update-theme-dto';

export interface UsersControllerUpdateTheme$Params {
      body: UpdateThemeDto
}

export function usersControllerUpdateTheme(http: HttpClient, rootUrl: string, params: UsersControllerUpdateTheme$Params, context?: HttpContext): Observable<StrictHttpResponse<ThemePreferencesResponseDto>> {
  const rb = new RequestBuilder(rootUrl, usersControllerUpdateTheme.PATH, 'patch');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ThemePreferencesResponseDto>;
    })
  );
}

usersControllerUpdateTheme.PATH = '/api/v1/users/me/preferences/theme';
