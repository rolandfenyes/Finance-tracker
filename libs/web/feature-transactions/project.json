{
  "name": "web-feature-transactions",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/web/feature-transactions/src",
  "tags": ["scope:web", "type:feature"],
  "targets": {
    "lint": { "executor": "@nx/eslint:lint" },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p libs/web/feature-transactions/tsconfig.lib.json --noEmit && tsc -p libs/web/feature-transactions/tsconfig.spec.json --noEmit"
      }
    }
  }
}
