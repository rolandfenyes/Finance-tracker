{
  "name": "web-shared",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/web/shared/src",
  "prefix": "mmm",
  "projectType": "library",
  "tags": ["scope:web", "type:shared"],
  "targets": {
    "build": {
      "executor": "@nx/angular:ng-packagr-lite",
      "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
      "defaultConfiguration": "production",
      "options": {
        "project": "libs/web/shared/ng-package.json",
        "tsConfig": "libs/web/shared/tsconfig.lib.json"
      },
      "configurations": {
        "production": {
          "tsConfig": "libs/web/shared/tsconfig.lib.prod.json"
        },
        "development": {}
      }
    },
    "test": {
      "executor": "@nx/angular:unit-test",
      "options": {
        "watch": false
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p libs/web/shared/tsconfig.lib.json --noEmit && tsc -p libs/web/shared/tsconfig.spec.json --noEmit"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
